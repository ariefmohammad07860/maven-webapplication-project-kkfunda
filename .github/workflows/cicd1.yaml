name: CICD Workflow1
on:
   workflow_dispatch
jobs:
   Build:
      runs-on: ubunut-latest
      steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
