name: Test SonarQube Token

on:
  workflow_dispatch:

jobs:
  test-sonar:
    runs-on: ubuntu-latest

    steps:
      - name: Check if SONAR_TOKEN is available
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: echo "Token length: ${#SONAR_TOKEN}"
